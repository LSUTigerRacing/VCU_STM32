# VCU_STM32
Firmware for STM32H723VGT6 
If you have to do any changes regarding the setup to the STM32 download the .ioc file and make the changes and generate the code. Make sure before you regenerate the code that your old code is within the commented lines for user code if not it will be deleted.
